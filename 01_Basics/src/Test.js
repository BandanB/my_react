function Test(){
    const fname = "Bandan"
    return(
        <h1>Checked sucessfully{fname}</h1>
    )
}
export default Test;

//Created js file with function name Test and export it to another file index.js
//we can pass variable name inside {}.only final computed outcome(evaluated expression) can be applied,means we cannot pass full code inside {}